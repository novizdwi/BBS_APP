@using (Ajax.BeginForm("mantap", "TransferSummaryOut", null,
new AjaxOptions
{
    HttpMethod = "POST",
    OnBegin = "OnBegin",
    OnSuccess = "OnSuccessMsgBox",
    OnFailure = "OnFailure",
    OnComplete = "OnComplete",
    UpdateTargetId = "CloseReason_contentForm",
    InsertionMode = InsertionMode.Replace,

},
new
{
    id = "CloseReason_formDetail",
    @class = "edit_form",
    style = "height: 200px; width: 100%;",
    autocomplete = "off"
}))
{
    <div id="CloseReason_contentForm">
        @Html.Partial("Partial/CloseReason/CloseReason_Panel_Partial")

    </div>
}

