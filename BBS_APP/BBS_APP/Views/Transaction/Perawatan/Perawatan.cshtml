@using Models._Cfl;


@if (false)
{
    <script src="~/Scripts/ASPxScriptIntelliSense.js" type="text/javascript"></script>
    <script src="~/Scripts/jquery-2.2.0.intellisense.js"></script>
    <script src="~/Scripts/Test/DXR.js"></script>
}

@model Models.Transaction.Perawatan.PerawatanModel


@{
    ViewBag.Title = "Perawatan Kapal";
}



<script type="text/javascript">


    $(document).ready(function () {

        SetBtnStatus();
    }
    );


</script>



<script type="text/javascript">

    var beforeSaveActiveTabName = '';
    var beforeSaveActiveTabIndex = 0;

    $.ajaxSetup({
        cache: false
    });

    function OnBegin() {
        loadingPanel.Show();

    }

    function SetBtnStatus() {


        var grpBtnAdd = formLayouBtn.GetItemByName('grpBtnAdd');
        var btnAdd = ASPxClientButton.Cast('btnAdd');
        var grpBtnUpdate = formLayouBtn.GetItemByName('grpBtnUpdate');
        var btnUpdate = ASPxClientButton.Cast('btnUpdate');


        var btnRefresh = ASPxClientButton.Cast('btnRefresh');

        var formMode = '';
        if ((Id.GetValue() == '') || Id.GetValue() == '0') {
            formMode = 'new';
        } else {
            formMode = 'edit';
        }

        switch (formMode) {
            case 'new':

                grpBtnAdd.SetVisible(true);
                grpBtnUpdate.SetVisible(false);

                btnAdd.SetEnabled(true);
                btnUpdate.SetEnabled(false);
                btnRefresh.SetEnabled(false);

                break;
            case 'edit':

                grpBtnAdd.SetVisible(false);
                grpBtnUpdate.SetVisible(true);


                btnAdd.SetEnabled(false);
                btnRefresh.SetEnabled(true);
                btnUpdate.SetEnabled(true);

                break;
            default:

                break;
        }



    }

    var msgCount = 0;
    function OnSuccess() {

        $("#MessageText").html('<p>' + '(' + (++msgCount) + ') ' + 'Action success</p>' + $("#MessageText").html());

    }


    function OnSuccessMsgBox() {

        $("#MessageText").html('<p>' + '(' + (++msgCount) + ') ' + 'Action success</p>' + $("#MessageText").html());
        loadingPanel.Hide();

        popupSuccess.Show();
        setTimeout(function () { popupSuccess.Hide(); }, 1000);

    }

    function OnFailure(jqXhr, textStatus, errorThrown) {
        if (beforeSaveActiveTabName != '') {
            var tab = ASPxClientTabControl.Cast(beforeSaveActiveTabName);
            tab.SetActiveTabIndex(beforeSaveActiveTabIndex);
            beforeSaveActiveTabName = '';
        }

        if (jqXhr.responseText.substring(0, 12) == "[VALIDATION]") {
            $("#MessageText").html('<p>' + '(' + (++msgCount) + ') ' + jqXhr.responseText + '</p>' + $("#MessageText").html());

            loadingPanel.Hide()
            alert(jqXhr.responseText);
        }
        else {
            // untuk error yg tidak ter capture di validation
            var doc = jqXhr.responseText.match(/.*<title>([\s\S]*)<\/title>.*/);
            if (doc != null) {
                $("#MessageText").html('<p>' + '(' + (++msgCount) + ') ' + doc[1] + '</p>' + $("#MessageText").html());

                loadingPanel.Hide();
                alert(doc[1]);
            }

        }

    }

    function OnComplete() {
        loadingPanel.Hide();
    }


    function BeforeSave() {

        var tab = ASPxClientTabControl.Cast("TabDetail");
        var tabPageCount = tab.GetTabCount();
        for (var i = 0; i < tabPageCount; i++) {
            tab.SetActiveTabIndex(i);
            if (!($("#formDetail").valid())) {
                var validator = $("#formDetail").validate();
                var errors = validator.numberOfInvalids();
                if (errors) {
                    // alert( validator.errorList[0].message);
                    validator.errorList[0].element.focus();

                }
                return false;
            }
        }

        return true;

    }


    function RefreshAfterSuccess(response) {
        //var tab = ASPxClientTabControl.Cast("TabDetail");

        //if (tab.GetActiveTabIndex != 0) {
        //    tab.SetActiveTabIndex(0);
        //}

        $("#contentForm").html(response);

        $.validator.unobtrusive.parse("#formDetail");

        MVCxClientUtils.FinalizeCallback();
        SetBtnStatus();
        //ExpiredTypeOnValueChange();
    }



    function OnClickBtnNew(s, e) {

        $.ajax({
            type: "POST",
            url: "@Url.Action("DetailPartial", "Perawatan")",
            beforeSend: function () {
                OnBegin();
            },

            success: function (response) {
                RefreshAfterSuccess(response);
                OnSuccess();
            },

            error: function (jqXhr, textStatus, errorThrown) {
                OnFailure(jqXhr, textStatus, errorThrown)
            },

            complete: function () {
                OnComplete()
            }
        });

    }


    function GetFormData() {
        var formData = $('#formDetail').serializeObject();

        var Details_ = GetGvDetails('gvDetail');
        $.extend(formData, { Details_: Details_ });
        return formData;
    }


    function OnClickBtnAdd(s, e) {
        if (BeforeSave() == false) {
            return;
        }

        var formData = GetFormData();/*$('#formDetail').serializeObject();*/
        console.log(formData);
        $.ajax({
            type: "POST",
            url: '@Url.Action("Add", "Perawatan")',
            data: formData,
            beforeSend: function () {
                OnBegin();
            },
            success: function (response) {
                RefreshAfterSuccess(response);
                OnSuccessMsgBox();
            },
            error: function (jqXhr, textStatus, errorThrown) {
                OnFailure(jqXhr, textStatus, errorThrown)
            },
            complete: function () {
                OnComplete();
            }
        });


    }

    function OnClickBtnUpdate(s, e) {
        if (BeforeSave() == false) {
            return;
        }

        var formData = GetFormData();

        $.ajax({
            type: "POST",
            url: '@Url.Action("Update", "Perawatan")',
            data: formData,
            beforeSend: function () {
                OnBegin();
            },
            success: function (response) {
                RefreshAfterSuccess(response);
                OnSuccessMsgBox();

            },
            error: function (jqXhr, textStatus, errorThrown) {
                OnFailure(jqXhr, textStatus, errorThrown)
            },
            complete: function () {
                OnComplete()

            }
        });



    }




    function OnClickBtnNavFirst(s, e) {
        $.ajax({
            type: "POST",
            url: "@Url.Action("NavFirst", "Perawatan")",
            beforeSend: function () {
                OnBegin();
            },
            success: function (response) {
                RefreshAfterSuccess(response);
                OnSuccess();
            },
            error: function (jqXhr, textStatus, errorThrown) {
                OnFailure(jqXhr, textStatus, errorThrown)
            },
            complete: function () {
                OnComplete()
            }
        });
    }

    function OnClickBtnNavPrevious(s, e) {
        key = Id.GetValue();

        $.ajax({
            type: "POST",
            url: '@Url.Action("NavPrevious", "Perawatan")',
            data: { Id: key },
            beforeSend: function () {
                OnBegin();
            },
            success: function (response) {
                RefreshAfterSuccess(response);
                OnSuccess();
            },
            error: function (jqXhr, textStatus, errorThrown) {
                OnFailure(jqXhr, textStatus, errorThrown)
            },
            complete: function () {
                OnComplete()
            }
        });
    }

    function OnClickBtnNavNext(s, e) {
        key = Id.GetValue();
        $.ajax({
            type: "POST",
            url: '@Url.Action("NavNext", "Perawatan")',
            data: { Id: key },
            beforeSend: function () {
                OnBegin();
            },
            success: function (response) {
                RefreshAfterSuccess(response);
                OnSuccess();
            },
            error: function (jqXhr, textStatus, errorThrown) {
                OnFailure(jqXhr, textStatus, errorThrown)
            },
            complete: function () {
                OnComplete()
            }
        });
    }



    function OnClickBtnNavLast(s, e) {
        $.ajax({
            type: "POST",
            url: "@Url.Action("NavLast", "Perawatan")",
            beforeSend: function () {
                OnBegin();
            },
            success: function (response) {
                RefreshAfterSuccess(response);
                OnSuccess();

            },
            error: function (jqXhr, textStatus, errorThrown) {
                OnFailure(jqXhr, textStatus, errorThrown)
            },
            complete: function () {
                OnComplete()
            }
        });
    }


    function OnClickBtnRefresh(s, e) {
        key = Id.GetValue();

        $.ajax({
            type: "POST",
            url: '@Url.Action("DetailPartial", "Perawatan")',
            data: { Id: key },
            beforeSend: function () {
                OnBegin();
            },
            success: function (response) {
                RefreshAfterSuccess(response);
                OnSuccess();

            },
            error: function (jqXhr, textStatus, errorThrown) {
                OnFailure(jqXhr, textStatus, errorThrown)
            },
            complete: function () {
                OnComplete()
            }
        });
    }



    function OnClickBtnListFind(s, e) {
        var popup = ASPxClientPopupControl.Cast("popupList");

        var gv = ASPxClientGridView.Cast('gvPerawatanList');
        if (gv == null) {
            popup.Show();
        } else {
            popup.SetContentHtml("");
            popup.PerformCallback();
            popup.Show();
        }
    }

    function OnClickBtnMenuOther(s, e) {

        switch (e.item.name) {

            case 'btnMenuSetStatusCancel':

                if (confirm('Cancel transaction?')) {
                    key = Id.GetValue();

                    $.ajax({
                        type: "POST",
                        url: '@Url.Action("Cancel", "Perawatan")',
                        data: { Id: key },
                        beforeSend: function () {
                            OnBegin();
                        },
                        success: function (response) {
                            RefreshAfterSuccess(response);
                            OnSuccessMsgBox();

                        },
                        error: function (jqXhr, textStatus, errorThrown) {
                            OnFailure(jqXhr, textStatus, errorThrown)
                        },
                        complete: function () {
                            OnComplete()
                        }
                    });
                }


                break;
            case 'btnMenuSetStatusPost':
                if (BeforeSave() == false) {
                    return;
                }
                var formData = GetFormData();
                if (confirm('Post transaction?')) {
                    key = Id.GetValue();

                    $.ajax({
                        type: "POST",
                        url: '@Url.Action("Post", "Perawatan")',
                        data: formData,
                        beforeSend: function () {
                            OnBegin();
                        },
                        success: function (response) {
                            RefreshAfterSuccess(response);
                            OnSuccessMsgBox();

                        },
                        error: function (jqXhr, textStatus, errorThrown) {
                            OnFailure(jqXhr, textStatus, errorThrown)
                        },
                        complete: function () {
                            OnComplete()
                        }
                    });
                }


                break;
            default:

                break;
        }
    }



    function OnClickChoose(s, e, key) {
        $.ajax({
            type: "POST",
            url: '@Url.Action("DetailPartial", "Perawatan")',
            data: { Id: key },
            beforeSend: function () {
                OnBegin();
            },
            success: function (response) {
                RefreshAfterSuccess(response);
                OnSuccess();
                popupList.Hide();
            },
            error: function (jqXhr, textStatus, errorThrown) {
                OnFailure(jqXhr, textStatus, errorThrown)
            },
            complete: function () {
                OnComplete()
            }
        });
    }

    function OnChangeCbWhs() {
        var whsName = WarehouseCode.GetText();
        WarehouseName.SetValue(whsName);
    }

    function BtnShipNameOnClick(s, e) {
        cflShipPopupListPerawatan.Show();
    }
    function BtnShipCodeOnClick(s, e) {
        cflTongkangPopupListPerawatan.Show();
    }

</script>


<script type="text/javascript">


    function OnBatchEditStartEditing(s, e) {
        //var Code_Column = s.GetColumnByField("ItemCode");
        //if (!e.rowValues.hasOwnProperty(Code_Column.index))
        //    return;
        //var cellInfo = e.rowValues[Code_Column.index];
        //gvItemLookup.SetValue(cellInfo.value);
        //if (e.focusedColumn === Code_Column)
        //    gvItemLookup.SetFocus();
    }

    function OnBatchEditEndEditing(s, e) {
        //var Code_Column = s.GetColumnByField("ItemCode");
        //if (!e.rowValues.hasOwnProperty(Code_Column.index))
        //    return;
        //var cellInfo = e.rowValues[Code_Column.index];
        //cellInfo.value = gvItemLookup.GetGridView().GetRowKey(gvItemLookup.GetGridView().GetFocusedRowIndex());
        //cellInfo.text = cellInfo.value;
        //gvItemLookup.GetGridView().GetRowValues(gvItemLookup.GetGridView().GetFocusedRowIndex()
        //    , "ItemCode;ItemName", function (result) {
        //        setColValue(s, e, result);
        //    });
    }

    function OnGridFocusedRowChanged(s, e) {
        //var grid = gvItemLookup.GetGridView();
        //var a = grid.GetRowValues(grid.GetFocusedRowIndex(), "ItemCode;ItemName", function (result) {
        //    setColValue(s, e, result, "ItemCode;ItemName");
        //});
    }

    function setColValue(s, e, value) {
        //s.batchEditApi.SetCellValue(e.visibleIndex, "ItemCode", value[0]);
        //s.batchEditApi.SetCellValue(e.visibleIndex, "ItemName", value[1]);

    }

    function OnLookupBeginCallback(s, e) {
    }
</script>
<script type="text/javascript">

    function gvDetailOnBeginCallback(s, e) {
        e.customArgs['cbId'] = Id.GetValue();
    }

    function gvDetailOnEndCallback(s, e) {

    }

    function gvDetailOnCustomButtonClick(s, e) {

        //if (e.buttonID == "deleteButton") {

        //    s.batchEditApi.StartEdit(e.visibleIndex);
        //    var rowValues = s.batchEditHelper.GetRowValues(e.visibleIndex);
        //    gvDetailCalculateSummary(s, rowValues, e.visibleIndex, true);
        //    s.DeleteRow(e.visibleIndex);
        //    s.GetBatchEditHelper().CanUpdate();

        //}
    }


    function gvDetailOnBatchEditStartEditing(s, e) {

        

    }
    function gvDetailOnBatchEditEndEditing(s, e) {

        

    }


    function gvDetailOnBatchEditRowValidating(s, e) {


    }

    function gvDetailCalculateSummary(grid, rowValues, visibleIndex, isDeleting) {

    }

    @*function gvDetail_Init(s, e) {
        var bagianComboBox = s.GetEditor("Bagian");

        if (bagianComboBox) {
            bagianComboBox.ClientSideEvents.SelectedIndexChanged = "function(s, e) { OnBagianChanged(s, e); }";
        }
    }

    function OnBagianChanged(s, e) {
        var selectedItem = s.GetSelectedItem();
        var itemComboBox = s.GetEditor("Item");

        if (selectedItem && itemComboBox) {
            var bagian = selectedItem.Bagian;

            // Panggil fungsi Ajax untuk memperbarui data "Item" berdasarkan "Bagian"
            updateItemDataSource(bagian, itemComboBox);
        }
    }

    function updateItemDataSource(bagian, itemComboBox) {
        // Panggil Ajax untuk mengambil data "Item" berdasarkan "Bagian"
        $.ajax({
            url: '@Url.Action("GetItemsByBagian", "Perawatan")',
            type: "GET",
            data: { bagian: bagian },
            success: function (response) {
                // Perbarui dataSource ComboBox "Item" dengan data yang baru
                itemComboBox.PerformCallback(response);
            },
            error: function (xhr, status, error) {
                // Tangani kesalahan jika terjadi
                console.error(error);
            }
        });
    }*@

</script>



@Html.Partial("Partial/Perawatan_Form_Btn_Partial")

@using (Ajax.BeginForm("mantap", "Perawatan", null,
new AjaxOptions
{
    HttpMethod = "POST",
    OnBegin = "OnBegin",
    OnSuccess = "OnSuccessMsgBox",
    OnFailure = "OnFailure",
    OnComplete = "OnComplete",
    UpdateTargetId = "contentForm",
    InsertionMode = InsertionMode.Replace,

},
new
{
    id = "formDetail",
    @class = "edit_form",
    style = "height: 200px; width: 100%;",
    autocomplete = "off"
}))
{
    <div id="contentForm" style="padding-top:79px">
        @Html.Partial("Partial/Perawatan_Form_Partial")

    </div>
}


@*
    -----------------------------------------------------------------------------
    loadingPanel
    -----------------------------------------------------------------------------
*@

@Html.DevExpress().LoadingPanel(
    settings =>
    {
        settings.Name = "loadingPanel";
        settings.Modal = true;
        settings.ContainerElementID = "contentForm";
        settings.Styles.LoadingDiv.BackColor = System.Drawing.Color.White;
        settings.Styles.LoadingDiv.Opacity = 0;
    }).GetHtml()



@*
    -----------------------------------------------------------------------------
    gvShipList
    -----------------------------------------------------------------------------
*@


@Html.Partial("Partial/Perawatan_Panel_List_Partial")


@{
    var cflShip_ParamModel = new CflShip_ParamModel();
    cflShip_ParamModel.Name = "Perawatan";
    cflShip_ParamModel.Type = "Perawatan";
    cflShip_ParamModel.Header = "";
    cflShip_ParamModel.SqlWhere = "";
    cflShip_ParamModel.IsMulti = "N";
}
<script type="text/javascript">


    function OnClickChoose_CflShipPerawatan(s, e, data) {
        ShipId.SetValue(data.Id);
        ShipCode.SetValue(data.ShipCode);
        ShipName.SetValue(data.ShipName);
        console.log(data);
        cflShipPopupListPerawatan.Hide();
        //DocumentTypeOnValueChanged();
    }


</script>
@Html.Partial("~/Views/_Cfl/_CflShip/Partial/_CflShip_Panel_List_Partial.cshtml", cflShip_ParamModel)

@{
    var cflTongkang_ParamModel = new CflTongkang_ParamModel();
    cflTongkang_ParamModel.Name = "Perawatan";
    cflTongkang_ParamModel.Type = "Perawatan";
    cflTongkang_ParamModel.Header = "";
    cflTongkang_ParamModel.SqlWhere = "";
    cflTongkang_ParamModel.IsMulti = "N";
}
<script type="text/javascript">


    function OnClickChoose_CflTongkangPerawatan(s, e, data) {

        console.log(data);

        gvDetail_ShipCode.SetValue(data.ShipCode);
        gvDetail_ShipName.SetValue(data.ShipName);
        gvDetail_ShipId.SetValue(data.Id);
        cflTongkangPopupListPerawatan.Hide();

    }


</script>
@Html.Partial("~/Views/_Cfl/_CflTongkang/Partial/_CflTongkang_Panel_List_Partial.cshtml", cflTongkang_ParamModel)


@Html.Partial("~/Views/_CrystalReport/Layout/Print_Panel_Partial.cshtml")

<script type="text/javascript">
    function gvPerawatanAttachmentDetailBeginCallback(s, e) {
        e.customArgs['cbId'] = Id.GetValue();
    }

    function gvPerawatanAttachmentDetail_OnClickBtnNew() {
        var popup = ASPxClientPopupControl.Cast("popupAttachment");
        popup.callbackUrl = '@Url.Action("Attachment_PopupListLoadOnDemandPartial", "Perawatan")' + '?Id=' + Id.GetValue(); // "/BB_TMS/Perawatan/Attachment_PopupListLoadOnDemandPartial?Id=" + Id.GetValue();

        popup.PerformCallback();


        popup.Show();
    }

    function Attachment_OnClickBtnUpload(s, e) {
        // Attachment_formDetail.action = '@Url.Action("Attachment_Upload", "Perawatan")';
        // $("#Attachment_formDetail").submit();

        UploadMultiFile.Upload();

    }

    function Attachment_OnClickBtnCancel(s, e) {
        var popup = ASPxClientPopupControl.Cast("popupAttachment");
        popup.Hide();

    }

    function Attachment_FileUploadComplete(s, e) {
        var popup = ASPxClientPopupControl.Cast("popupAttachment");
        popup.Hide();
        gvPerawatanAttachmentDetail.PerformCallback();;
    }

    function gvPerawatanAttachmentDetail_OnClickBtnDelete(DetId) {
        if (confirm('Delete data?')) {
            var gv = ASPxClientGridView.Cast("gvPerawatanAttachmentDetail");
            gv.DeleteRowByKey(DetId);
        }
    }




</script>

@Html.Partial("Partial/Attachment/Attachment_Panel_Partial")